# Foundry

The foundry is the small common layer shared by every capability pack. It
defines how packs describe themselves and how agents turn bespoke work into
reusable, discoverable operations.

It deliberately does not provide a universal runtime. Each pack may use the
language and native interfaces best suited to its target application.
